You need to unzip the file then run npm install in the directory in which you unziped it.

After installing the dependencies you can use the "npm test" command to run the tests.

You can also use the "npm run coverage" to generate a coverage report.

